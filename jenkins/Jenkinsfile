pipeline {
    agent any

    stages {
        stage('Run backend unit tests') {
            steps {
                dir('bugtracker-backend') {
                   sh 'go test -v ./...'
                }
            }
        }
    }
}